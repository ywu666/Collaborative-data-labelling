To run the backend, you need the following libraries:

Flask
Flask CORS
pytest
pymongo
python-dotenv
dnspython

If you have pip installed, run these commands in order:

python -m venv venv
pip install -r requirements.txt